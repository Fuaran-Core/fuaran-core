{
  "kind": "proofs",
  "subject": "fixture",
  "$comment": "A FIXTURE for the Phase 174 classification clauses (see ladder-ok.json). ONE defect: a `model-bridge` carries no `closes`, so a reader cannot tell a gap nobody has taken from one nothing could ever close.",
  "claims": [
    {
      "id": "fixture-theorem",
      "level": "proved",
      "claim": "A clean proved row, so the every-module clause stays quiet.",
      "phase": "fuaran-core#144",
      "evidence": {
        "theorem": "alpha",
        "model": "Model.fst",
        "prover": "fixture"
      }
    },
    {
      "id": "fixture-bridge",
      "level": "assumed",
      "claim": "A model bridge carrying no closes.",
      "phase": "fuaran-core#174",
      "class": "model-bridge",
      "evidence": {
        "assumption": "the fixture model reads a carrier the extraction cannot represent"
      }
    }
  ]
}
