{
  "kind": "proofModules",
  "subject": "Fuaran-Core",
  "$comment": "What the proof leg COSTS, declared per module, so that a growth in prover time is named rather than discovered when CI times out. `proofs/check.ps1` reads this file: every green CHECK line prints the module's measured seconds beside its budget, and a run over budget prints a named COST warning — never a failure on its own, because prover time varies by machine and by load and a budget is a smoke detector rather than a gate. `check.ps1 -Strict` promotes the warnings to a red leg for a session that wants one — since Phase 171 the UNLABELLED ones only, see `contentionSeeding` below. This is NOT `proofs.json`: that file is the claims ladder — what is proved, tested and assumed, at a closed set of levels, checked row-by-row against the tree by the Proofs.Ladder family — and a wall-clock budget is a claim at none of those levels and is not per-claim but per-module. The ladder's `proof-leg` policy row points here for the cost half. Coverage is checked BOTH ways: a module the leg checks with no entry here, and an entry here for a module the leg does not check, are each a COST warning naming what to do. Hand-authored; the seeding rule below says how a number is arrived at, and bumping one is the recorded act the README's `Running it` section describes. SINCE PHASE 164 each entry also carries a `floorSeconds`, and that number is a GATE rather than a smoke detector: a module that verifies in less than its floor fails the leg on the spot, because a run faster than the module can possibly be is not evidence of speed but of a cache that was not cold. The two rules sit in `seeding` and `floorSeeding` below, and the asymmetry between warning above and failing below is argued in the second of them. SINCE PHASE 166 not every timed line in the log is a candidate measurement: an ABORTED module (the prover died with nothing to say, which is a different verdict from a refutation) is retried once in the same run, and the retry runs against a cache the aborted attempt had already half-filled. `check.ps1` compares that line to NEITHER number here and marks it `(warm cache: NOT a cold measurement)`, and the leg's closing verdict names every abort it retried. DO NOT SEED A BUDGET OR A FLOOR FROM A RETRY LINE — both rules below say `cold run` and mean it, and a budget seeded from a warm one is a ceiling nothing can reach while a floor seeded from a warm one is a red leg on the next honest machine. SINCE PHASE 171 a third block, `contentionSeeding`, says when an overshoot is about the MACHINE rather than about the module: the leg reports a per-run CONTENTION FACTOR read off the modules the tree did not change, labels every cost finding from a run above the threshold, and a labelled finding is NOT a re-seed obligation — so DO NOT SEED A BUDGET FROM A LABELLED RUN EITHER, which is the judgement `TreeOps`'s and `DocVocabularyProofs`'s notes below had to make by hand.",
  "seeding": {
    "rule": "budgetSeconds = max(2 x the slowest observed cold-cache run, minimumBudgetSeconds), rounded up to the next 10s.",
    "toleranceFactor": 2,
    "minimumBudgetSeconds": 20,
    "$comment": "The tolerance is applied ONCE, here, when a number is seeded — `check.ps1` multiplies nothing, so the pair it prints is the measurement against the ceiling and there is no second arithmetic to hold in your head. Seeded from the SLOWEST observed cold run rather than the median, deliberately: on the machine these were measured on, six concurrent sessions inflate a cold check by ~1.8x with nothing about the tree changed (TreeOps 45s to 77s, JsonParse 52s to 99s within one three-run leg), so a budget seeded from the median would fire on a busy afternoon and teach a reader to ignore it. The 20s minimum is for the sub-5s modules, where 2x is inside ordinary process-start noise. These are figures from a Windows dev machine; CI's windows-latest runner is a different machine and its first runs are what will say whether the tolerance is generous enough there. If it is not, that is a bump, and a bump is recorded.",
    "$namingNote": "RENAMED by Phase 155, from `floorSeconds`. That was the same word the per-entry time floor uses for the opposite thing — this one is the smallest a BUDGET may be (a floor under the CEILING), an entry's `floorSeconds` is the smallest a RUN may be, and the `Skeleton` entry below had to spend two sentences telling the two apart. Renamed rather than documented, because Phase 155 ships this file's skeleton in `proofs/kit/templates/modules.json` and a skeleton that carries a collision ships it to everyone who copies it. Nothing reads this key but people: `check.ps1` reads only the `modules` array, so the rename moved four prose readers and no code."
  },
  "floorSeeding": {
    "rule": "floorSeconds = the FASTEST genuine cold run ever observed, halved, rounded DOWN to the whole second; 0 for a module whose fastest genuine cold run is under 5s.",
    "divisor": 2,
    "zeroBelowSeconds": 5,
    "$comment": "The floor is the budget's mirror and the two are deliberately NOT symmetric in what they do. A budget is a ceiling and a smoke detector: an overshoot is a true measurement of a true cost, so it warns and the leg stays green. A floor is a gate: `check.ps1` FAILS the leg on the spot, naming the module and the time, because a module that verified faster than it can possibly verify has not told you it is fast — it has told you the measurement is worthless, and everything measured after it shares the apparatus. Implausibly fast is the one direction in which a lie looks like good news. WHAT BOUGHT THIS (2026-09-14, recorded in Phase 143's outcome): an orphaned background `check.ps1` kept writing the then-shared `proofs/obj/cache` while a replacement run started in the same worktree; the replacement found the orphan's `.checked` files, reported `TreeOps 0s`, `Skeleton 0s`, `Chain 0s` and printed `==== proofs: green`. It was caught by a human reading the timings. Phase 164 closed the cause (a per-invocation cache directory, `obj/cache-<pid>`) and this file closes the class (a floor, so a shared cache anywhere else still cannot pass as evidence). Seeded from the FASTEST observed rather than the median, and halved on top, for the mirror of the budget's reason: a floor that fires on a fast quiet machine teaches a reader to ignore it, and the failure this catches lands at or near ZERO seconds, so an enormous margin costs nothing in detection. The 5s rule is for the modules where halving lands inside process-start noise — `Skeleton` checks in about one second, and any positive floor there would be a coin toss. An ABSENT `floorSeconds` is a cost finding rather than a failure (a sibling adding a model should no more go red for a floor nobody has measured than for a budget nobody has measured); an explicit 0 is a different statement and reads as one. `check.ps1 -NoFloor` is the deliberate opt-out for a machine genuinely that fast, and it says so in the log. Re-seeding a floor DOWNWARDS is the same recorded act as bumping a budget upwards: the new number, the `fastestSeconds` it came from, and your phase. One asymmetry makes that act rarer than the budget's: a run FASTER than the recorded `fastestSeconds` but still above the floor leaves the floor correct and merely conservative, so it obliges nothing — where a run over budget is a live finding every time. Only a breach forces the number, and a breach is first a reason to look for a second writer. The figures here were seeded on 2026-09-14 from the fastest of three cold passes on one quiet Windows dev machine; CI's runner is a different machine, and if it is genuinely faster than half of these, that is a re-seed and a re-seed is recorded."
  },
  "contentionSeeding": {
    "rule": "threshold = a number above the median ratio of a measured QUIET pass on the SLOWEST machine that runs this leg, and at or below the median ratio of the contention worth naming. Place it high: a false label suppresses a real regression, where a missed label costs only what the leg did before.",
    "threshold": 0.8,
    "minimumSamples": 3,
    "quietFactor": 0.29,
    "quietPhase": "fuaran-core#171",
    "$comment": "SINCE PHASE 171 the leg reports, at the end of every run, the MEDIAN over the modules the working tree did NOT change of that module's measured seconds divided by the `measuredSeconds` its entry below records — the run's CONTENTION FACTOR. Above this threshold every cost finding from that run is LABELLED a contended pass where the closing verdict prints it, and a labelled finding is explicitly NOT a re-seed obligation: re-seeding a budget from a contended run raises a ceiling to fit a slow afternoon, which is exactly how a budget stops meaning anything. `check.ps1 -Strict` promotes only the UNLABELLED findings. Nothing is multiplied into a measurement — the seconds a green line prints stay the wall clock, and the factor sits beside them. THE SCALE IS NOT THE OBVIOUS ONE AND WAS MEASURED, NOT ASSUMED (Phase 171): `measuredSeconds` is not a typical cost, it is by `seeding`'s own rule the SLOWEST cold run ever observed for that module, and most of the entries below were seeded under six concurrent sessions — so the ratio's neutral point sits well BELOW one. A quiet cold pass of the whole leg on this dev machine measured x0.29 over sixteen contributing modules (DagFold 14s, WireDecode 9s, TreeOps 19s, Chain 4s, JsonParse 19s, Preservation 13s, TreeDiff 4s, WireCanon 9s, Vocabulary 3s, VocabularyProofs 5s, DocVocabulary 4s, DocVocabularyProofs 7s, ScoreVocabulary 7s, ScoreVocabularyProofs 14s, ColumnOps 12s, Capability 6s; machine idle, nothing else on it), and the SAME leg under four sustained competing provers measured x0.40 (every large module 1.4-1.7x its quiet time). The three incidents this threshold exists to name — Chain's 35s/31s against 30s, Phase 162's TreeOps at 145s, Phase 182's Capability at 75s against 20s — all ran beside six concurrent SESSIONS, which is the load `measuredSeconds` itself was seeded under, so they sit near x1.0 by construction. 0.8 is therefore placed between: 2.7x above this machine's quiet pass, so a machine up to 2.7x slower still reads an ordinary pass as ordinary, and below the x1.0 the recorded incidents produce. WHAT THAT COSTS, said plainly: four bare competing provers on this machine (x0.40) is NOT labelled, so the threshold does not fire on mild contention — deliberate, because a false label is the expensive error and a missed one leaves the reader exactly where Phase 148 left them. THE OPEN QUESTION is the same one `seeding` records for its own tolerance: the baseline is machine-dependent, CI's windows-latest runner is a different and slower machine, and its first runs are what will say whether 0.8 clears ITS quiet pass. If it does not, that is a re-seed and a re-seed is recorded — the new number, the quiet factor it came from, and the phase. `minimumSamples` is the smallest set the median may be taken over; below it the leg reports the factor as not computed rather than taking a median of one. The per-module cut — a module whose `measuredSeconds` is under `floorSeeding.zeroBelowSeconds` does not vote, because at whole-second resolution 0s against a recorded 2s is a ratio of 0 and says nothing about the machine — is that rule's number reused rather than a second one minted here; it excludes Skeleton, Limits and WireVersioning. Each entry below may also carry an optional `contentionFactor` beside its `measuredSeconds`, recorded by whichever phase next seeds that number: it says what the machine was doing when the measurement was taken, so a later reader can tell a budget seeded quiet from one seeded busy. It is PROVENANCE ONLY — `check.ps1` holds it to its shape and computes nothing from it, for the same reason the factor is never multiplied into a measurement — and an absent one reads as `not recorded`, never as 1. No entry below carries one yet: this phase re-seeds no budget, and back-filling a figure for a pass nobody measured would be inventing the very provenance the field exists to carry."
  },
  "modules": [
    {
      "module": "DagFold",
      "budgetSeconds": 130,
      "measuredSeconds": 65,
      "floorSeconds": 9,
      "fastestSeconds": 18,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#156",
      "note": "The first model, and the one section 11's delta recovery lives in. It carries no `--ext context_pruning`; what it carries instead is the SMT-pattern lesson (see the README's per-module costs) — `rev` / `app` / `ids_of` / `ops_of` rewrite into one another and their lemmas deliberately have NO `SMTPat`. Adding one back is the cost cliff here, and it is a cliff rather than a slope: the query stops returning. BUMPED by fuaran-core#156 from 100s/48s: section 13 added the general drain — the id-ordered frontier drain over an abstract node set, three theorems, and three inductions over the drain's own accumulator (`kahn_extends`, `kahn_complete`, `kahn_det`) with about twenty list lemmas under them. Seeded from FIVE cold observations taken across the phase: 64s standalone, then 58s, 54s, 62s and 65s across four cold passes of the leg, so the rule's slowest observed is 65s and 2x65 rounds to 130. Three of the new lemmas carry a scoped `#push-options \"--z3rlimit 200\"` or `250`; if this budget is ever bumped again, check those are still SCOPED rather than hoisted to the file, because a file-wide rlimit would inflate every query in sections 0-12 as well and would read as ordinary growth — the shape `TreeOps`'s entry warns about for `--ext context_pruning`. The Phase 164 FLOOR is deliberately left at 12s/24s: the module got slower and never faster, so the floor stays correct and merely conservative, and `floorSeeding`'s own rule forces a re-seed only on a breach. FLOOR RE-SEEDED BY PHASE 173: cold quaked runs on a dev machine across four `check.ps1 -Runs 3` legs (23s, 33s, 18s, 18s, 23s, 25s, 33s, 27s) came in under the 24s fastest this entry had recorded — the seeding runs were taken under six concurrent sessions, and on the idle legs every module but WireVersioning (a 2s module that measured 7s once) ran faster than its seeding run, with two floors firing on the way (Chain at 4s, WireCanon at 13s), exactly as the gate is designed to. Nothing shared the cache (per-invocation directory, no other prover process). Fastest genuine cold run is therefore 18s, floor 9s by floorSeeding; the budget stands, since nothing observed was slower than the run it was seeded from."
    },
    {
      "module": "WireDecode",
      "budgetSeconds": 70,
      "measuredSeconds": 33,
      "floorSeconds": 4,
      "fastestSeconds": 9,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#148",
      "note": "The decode combinators. Default context, no scoped options; the cost is ordinary and the spread between an idle and a contended machine (15s to 33s) is most of it. FLOOR RE-SEEDED BY PHASE 173: cold quaked runs on a dev machine across four `check.ps1 -Runs 3` legs (15s, 11s, 11s, 12s, 12s, 14s, 15s, 14s, 9s) came in under the 12s fastest this entry had recorded — the seeding runs were taken under six concurrent sessions, and on the idle legs every module but WireVersioning (a 2s module that measured 7s once) ran faster than its seeding run, with two floors firing on the way (Chain at 4s, WireCanon at 13s), exactly as the gate is designed to. Nothing shared the cache (per-invocation directory, no other prover process). Fastest genuine cold run is therefore 9s, floor 4s by floorSeeding; the budget stands, since nothing observed was slower than the run it was seeded from."
    },
    {
      "module": "TreeOps",
      "budgetSeconds": 200,
      "measuredSeconds": 99,
      "floorSeconds": 11,
      "fastestSeconds": 22,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#162",
      "note": "Carries `#set-options \"--ext context_pruning\"` near the top — 300s with the default context against 56s pruned, measured on the pinned prover. The single most expensive module in the leg even so, because it is an order of magnitude larger than DagFold. If this budget is ever bumped, check first that the pruning option is still in force: losing it is a 5x regression that looks like ordinary growth in the log. BUMPED by fuaran-core#162 from 160s/77s: sections 19 and 20 added the preorder-position lemma and the batch lift, some five hundred lines, and the lift is a mutual induction over the operation type and over lists of it — the shape that costs, because every case of it meets the module's whole membership algebra. Seeded over FIVE cold runs measuring 92s, 99s, 75s, 88s and 145s, and the number is 2x the 99s rather than 2x the 145s — which is a departure from the rule as written, so here is the reason rather than a silent choice. The 145s run inflated three UNTOUCHED modules by the same factor in the same pass (DagFold 58s against 35-49s, JsonParse 164s against 86-132s, Chain 31s against 16-23s), so it measures the afternoon and not this module; 2x145 would be 290s, a ceiling nothing on this machine could ever reach, and a detector that cannot fire is not one. 200s covers the worst run observed with room and still fires on a real regression. No COST finding fired at the OLD number on any of the five, which is the point of re-seeding here rather than waiting: the budget had been left behind by the module underneath it, and the first run to say so would have been someone else's contended afternoon. The Phase 164 FLOOR is deliberately left at 19s/38s: every run this phase measured is slower than the 38s it was seeded from, so the floor stays correct and merely conservative, and `floorSeeding`'s own rule forces a re-seed only on a breach. FLOOR RE-SEEDED BY PHASE 173: cold quaked runs on a dev machine across four `check.ps1 -Runs 3` legs (23s, 22s, 24s, 24s, 27s, 28s, 24s, 25s) came in under the 38s fastest this entry had recorded — the seeding runs were taken under six concurrent sessions, and on the idle legs every module but WireVersioning (a 2s module that measured 7s once) ran faster than its seeding run, with two floors firing on the way (Chain at 4s, WireCanon at 13s), exactly as the gate is designed to. Nothing shared the cache (per-invocation directory, no other prover process). Fastest genuine cold run is therefore 22s, floor 11s by floorSeeding; the budget stands, since nothing observed was slower than the run it was seeded from."
    },
    {
      "module": "Skeleton",
      "budgetSeconds": 20,
      "measuredSeconds": 2,
      "floorSeconds": 0,
      "fastestSeconds": 0,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#148",
      "note": "The composite, thin on purpose — the argument is in the two halves it joins. At the 20s minimum: 2x of a 2s measurement is inside process-start noise, so `seeding.minimumBudgetSeconds` is what keeps this entry from being a false-alarm generator. (That 20s is the BUDGET rule's minimum ceiling, and is a different thing from this entry's `floorSeconds`, which is the Phase 164 per-module time floor and is 0 here. The two were both spelled `floorSeconds` until Phase 155 renamed the first one, which is why this parenthesis is shorter than it was.) The 0 is the whole reason `floorSeeding` has a `zeroBelowSeconds`: this module genuinely checks in about one second, so half of its fastest run is under a second and any positive floor would be decided by process-start noise rather than by whether the cache was cold. It is therefore the ONE module in the leg that a shared cache could still make implausibly fast without this file noticing — the per-invocation cache directory in `check.ps1` is what covers it, and the floor here is honest about covering nothing. FLOOR RE-SEEDED BY PHASE 173: cold quaked runs on a dev machine across four `check.ps1 -Runs 3` legs (0s, 0s, 0s, 0s, 1s, 1s, 0s, 0s) came in under the 1s fastest this entry had recorded — the seeding runs were taken under six concurrent sessions, and on the idle legs every module but WireVersioning (a 2s module that measured 7s once) ran faster than its seeding run, with two floors firing on the way (Chain at 4s, WireCanon at 13s), exactly as the gate is designed to. Nothing shared the cache (per-invocation directory, no other prover process). Fastest genuine cold run is therefore 0s, floor 0s by floorSeeding; the budget stands, since nothing observed was slower than the run it was seeded from."
    },
    {
      "module": "Chain",
      "budgetSeconds": 30,
      "measuredSeconds": 12,
      "floorSeconds": 0,
      "fastestSeconds": 4,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#148",
      "note": "The two integrity walkers. Default context; 2x of 12s rounds to the floor's neighbourhood anyway. RE-SEEDED BY PHASE 173: a cold quaked run on an otherwise idle dev machine verified it in 4s against the 10s fastest this entry had recorded (DagFold 23s, WireDecode 15s and TreeOps 23s in the same leg — every module faster than its seeding run, none under its floor but this one), and the floor gate fired exactly as designed. Nothing shared the cache (per-invocation directory, no other prover process); the machine was simply faster than the seeding machine by a factor the 2x floor did not cover on a module this small. Fastest genuine cold run is therefore 4s, which is under the zeroBelowSeconds clause, so the floor is 0 — honest about covering nothing, like Skeleton's, Limits's and WireVersioning's, and covered by the per-invocation cache directory instead."
    },
    {
      "module": "JsonParse",
      "budgetSeconds": 200,
      "measuredSeconds": 99,
      "floorSeconds": 11,
      "fastestSeconds": 22,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#148",
      "note": "Carries `#set-options \"--ext context_pruning\"` at line 98 — 128s default against 72s pruned. The widest spread in the leg (52s to 99s across one three-run pass), because a 45-constructor alphabet in a large mutual group is exactly the shape a loaded machine punishes; the budget is sized for the top of that spread and not its middle. FLOOR RE-SEEDED BY PHASE 173: cold quaked runs on a dev machine across four `check.ps1 -Runs 3` legs (22s, 28s, 26s, 24s, 37s, 22s, 25s) came in under the 41s fastest this entry had recorded — the seeding runs were taken under six concurrent sessions, and on the idle legs every module but WireVersioning (a 2s module that measured 7s once) ran faster than its seeding run, with two floors firing on the way (Chain at 4s, WireCanon at 13s), exactly as the gate is designed to. Nothing shared the cache (per-invocation directory, no other prover process). Fastest genuine cold run is therefore 22s, floor 11s by floorSeeding; the budget stands, since nothing observed was slower than the run it was seeded from."
    },
    {
      "module": "Preservation",
      "budgetSeconds": 190,
      "measuredSeconds": 93,
      "floorSeconds": 7,
      "fastestSeconds": 15,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#140",
      "note": "The apply engine, seeded by the phase that added the model over SIX cold runs, two `-Runs 3` passes: 39s, 36s, 27s and then 25s, 42s, 32s. The slowest is the 42s, and it is the one the rule takes — it was measured with a sibling session's full repo gate running beside it on the same machine, which is precisely the contention the tolerance exists to absorb rather than a run to discard. 2x42 rounds to 90s. Seeding from the first pass alone would have given 80s, which every run still fits; the number moved because the rule says slowest observed, and a rule applied only to the convenient half of the data is not the rule. Carries `#set-options \"--ext context_pruning\"` like the two modules above, and one SCOPED exception beside it that this budget depends on: `invert_applicable` proves 78 goals in a single query and sits under `#push-options \"--z3rlimit 200\"`, because at the leg's default 40 it was close enough to the ceiling that a different `--quake` seed exhausted it — green standalone, red under check.ps1. If this module ever needs a large bump, check that BOTH are still in force before believing the growth is ordinary: losing the scoped rlimit does not read as slowness, it reads as a flake. BUMPED by fuaran-core#140 from 90s/42s: section 8 added the container capability — a second engine clause for clause, the invariant, two premise refutations and the `--quake`-proved lemmas over them. Seeded from a `-Runs 3` pass measuring 85s, 93s and 65s, so the rule's SLOWEST observed is the 93s and 2x93 rounds to 190. Two things about that are worth the sentence. The first draft of this bump used the 71s a single earlier run had measured and set 150s — which is the rule applied to the convenient half of the data, exactly what the seeding note above warns against, and the 93s run happened in the very pass that would have shipped it. The second is that the 93s run EXCEEDED the old 90s budget: this is not a ceiling raised to fit a green run, it is a budget that had genuinely been left behind by the module underneath it. FLOOR RE-SEEDED BY PHASE 173: cold quaked runs on a dev machine across four `check.ps1 -Runs 3` legs (17s, 26s, 25s, 19s, 22s, 15s, 53s) came in under the 33s fastest this entry had recorded — the seeding runs were taken under six concurrent sessions, and on the idle legs every module but WireVersioning (a 2s module that measured 7s once) ran faster than its seeding run, with two floors firing on the way (Chain at 4s, WireCanon at 13s), exactly as the gate is designed to. Nothing shared the cache (per-invocation directory, no other prover process). Fastest genuine cold run is therefore 15s, floor 7s by floorSeeding; the budget stands, since nothing observed was slower than the run it was seeded from."
    },
    {
      "module": "TreeDiff",
      "budgetSeconds": 80,
      "measuredSeconds": 37,
      "floorSeconds": 0,
      "fastestSeconds": 4,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#162",
      "note": "The diff — `Diff.toOps`'s two refusals, the four passes, and the container-aware mirror. Seeded over EIGHT cold runs: 9s, 10s, 9s, 9s, 9s before the Phase 161 merge and 12s, 9s, 9s after it, so the rule's slowest observed is 12s, 2x is 24s and the next 10s is 30. It was first written as the 20s seeding minimum off the first five runs, and the 12s arrived in the very pass that would have shipped that number — the same shape `Preservation`'s entry below records, and the reason the rule says slowest rather than typical. Nothing regressed: the 12s run is inside the old 20s too, and the module is unchanged between the two passes; what moved is which runs the rule had seen. It is the cheapest model in the leg after `Skeleton`, and the reason is worth knowing before anyone reads a future bump as ordinary growth: what is proved here is what the four passes GUARANTEE ABOUT THE SCRIPT — shape, order and the refusal characterisation — and not what applying that script DOES, which the README's theorem 6 carries at level 2 instead. The lemmas are therefore structural inductions over lists, which Z3 discharges quickly; the reconstruction theorem, if it is ever taken, reasons about `ins` and `rem_at` over intermediate trees and would put this module in `Preservation`'s cost class rather than `Chain`'s. Carries `#set-options \"--ext context_pruning\"`, like the three modules above and for the same reason — it opens `TreeOps` and `DagFold`, whose ninety-odd patterned lemmas would otherwise be live at every query here. BUMPED by fuaran-core#162 from 30s/12s: section 9 added the positional facts over the preorder-position lemma, which are the module's first inductions over the TREE rather than over a list — the prediction in the sentence above, arriving for the positional half and not for the reconstruction half. Seeded over FOUR cold runs measuring 13s, 21s, 13s and 37s: the rule's slowest observed is the 37s, 2x37 is 74 and the next 10s is 80. Where that 37s came from is worth knowing before anyone reads a future figure as ordinary growth — it was measured in a pass that spiked machine-wide, with `JsonParse` at 132s against its usual 86-105 and `Preservation` at 127s against its usual 57-65 in the same run, on a machine carrying six concurrent proof sessions. The rule takes the slowest observed anyway, deliberately and for the reason its own note gives; recording the provenance is how the next reader tells a slow afternoon from a regression. The Phase 164 FLOOR is deliberately left at 4s/8s: every run this phase measured is slower than the 8s it was seeded from, so the floor stays correct and merely conservative, and `floorSeeding`'s own rule forces a re-seed only on a breach. FLOOR RE-SEEDED BY PHASE 173: cold quaked runs on a dev machine across four `check.ps1 -Runs 3` legs (5s, 9s, 16s, 7s, 5s, 4s, 17s) came in under the 8s fastest this entry had recorded — the seeding runs were taken under six concurrent sessions, and on the idle legs every module but WireVersioning (a 2s module that measured 7s once) ran faster than its seeding run, with two floors firing on the way (Chain at 4s, WireCanon at 13s), exactly as the gate is designed to. Nothing shared the cache (per-invocation directory, no other prover process). Fastest genuine cold run is therefore 4s, floor 0s by floorSeeding; the budget stands, since nothing observed was slower than the run it was seeded from."
    },
    {
      "module": "Limits",
      "budgetSeconds": 20,
      "measuredSeconds": 1,
      "floorSeconds": 0,
      "fastestSeconds": 0,
      "floorPhase": "fuaran-core#149",
      "phase": "fuaran-core#149",
      "note": "The WIRE_FORMAT section-21 resource limits as named premises. Eight constants and two lemmas about the relations between them, so there is nothing here for the prover to work at: both cold runs measured under a second and the entry sits at the 20s floor for exactly the reason the seeding note gives — 2x of a 1s measurement is inside process-start noise, and a budget that fires on a busy afternoon teaches a reader to ignore it. If this one ever overshoots, the module has stopped being a table of constants and the growth is the finding. Its floorSeconds is 0 by the floorSeeding rule's zeroBelowSeconds clause, for Skeleton's reason and more so: the fastest cold run measured was under a second, so half of it is inside process-start noise and any positive floor here would be a coin toss. Like Skeleton's, this 0 is honest about covering nothing — what covers this module against a warm cache is the per-invocation cache directory Phase 164 put in check.ps1, not this number."
    },
    {
      "module": "WireCanon",
      "budgetSeconds": 180,
      "measuredSeconds": 86,
      "floorSeconds": 6,
      "fastestSeconds": 12,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#149",
      "note": "The canonical encoder, its reader, and the canonical form proved in both directions. Seeded over two cold `--quake 3` runs measuring 86s and 40s, so the rule's SLOWEST observed is the 86s and 2x86 rounds to 180. The spread is the widest in the leg after JsonParse's and has the same cause: the 86s run had a sibling session's full repo gate running beside it, which is precisely the contention the tolerance absorbs rather than a run to discard. Carries `#set-options \"--ext context_pruning\"` like the four modules above, and for the same reason it matters here in particular: the reader is a mutual group whose termination rides refinements in the return types, the shape this file's own note names as the expensive one under the default context. Two things to check before reading a future bump as ordinary growth. The first is that the pruning option is still in force. The second is specific to this module: the reader exists because a LEFT INVERSE is the cheap route to injectivity, and the alternative — a direct first-difference induction over the rendered byte lists — is a different cost class entirely, so a rewrite that removed the reader would not read in the log as a rewrite. Its floorSeconds is seeded per Phase 164's rule from the fastest genuine cold run of the two, 40s, halved: 20. That is the same figure JsonParse carries and for the same reason — these are the two large mutual groups in the leg, and neither can plausibly verify in a quarter of a minute on any machine this decade. The 86s run is not the seed for this number even though it is the seed for the budget: the two rules read opposite ends of the same observations on purpose, and applying either to the convenient end is how both stop meaning anything. FLOOR RE-SEEDED BY PHASE 173: cold quaked runs on a dev machine across four `check.ps1 -Runs 3` legs (13s, 19s, 17s, 15s, 12s, 14s, 31s) came in under the 40s fastest this entry had recorded — the seeding runs were taken under six concurrent sessions, and on the idle legs every module but WireVersioning (a 2s module that measured 7s once) ran faster than its seeding run, with two floors firing on the way (Chain at 4s, WireCanon at 13s), exactly as the gate is designed to. Nothing shared the cache (per-invocation directory, no other prover process). Fastest genuine cold run is therefore 12s, floor 6s by floorSeeding; the budget stands, since nothing observed was slower than the run it was seeded from."
    },
    {
      "module": "WireVersioning",
      "budgetSeconds": 20,
      "measuredSeconds": 2,
      "floorSeconds": 0,
      "fastestSeconds": 2,
      "floorPhase": "fuaran-core#151",
      "phase": "fuaran-core#151",
      "note": "WIRE_FORMAT section 15.4's evolution-policy table as theorems about the IDL diff: the classifier proved sound, an additive step proved unobservable to a document that predates it, must-ignore-but-preserve proved at the BYTES, and the transport-only Unknown proved un-constructible from an encoder. Carries `#set-options \\\"--ext context_pruning\\\"` INSIDE the module, like the five above it, and here the reason is unusually clean: it opens WireCanon for four definitions and two lemmas, while WireCanon's own ninety-odd patterned facts about sorting, rendering and reading would otherwise be live at every query — and this module's own queries are small list inductions that need none of them. At the 20s seeding minimum: two cold quaked runs measured 2s and 4s (the 4s one with no dependency cache at all, which is not how the leg runs it), and 2x of either is inside process-start noise. The module is cheap because the two inductions are over lists of tags and everything else is a case split once they are in hand — which is a fact about where the difficulty in this phase sat, and it did not sit in the prover. If this one ever overshoots, check FIRST that the pruning option is still in force and SECOND whether a theorem was added that reasons about `render` rather than merely quoting it: the byte claim here is discharged by two WireCanon lemmas and does not re-enter that module's cost class, and a theorem that did would not read in the log as a change of kind. Its floorSeconds is 0 by the floorSeeding rule's zeroBelowSeconds clause, for the reason Skeleton's and Limits's are: the fastest cold run is under 5s, so half of it is inside process-start noise and any positive floor would be a coin toss. Like theirs, this 0 is honest about covering nothing — what covers this module against a warm cache is the per-invocation cache directory Phase 164 put in check.ps1."
    },
    {
      "module": "Vocabulary",
      "budgetSeconds": 20,
      "measuredSeconds": 10,
      "floorSeconds": 0,
      "fastestSeconds": 3,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#173",
      "note": "The GENERATED model of the engine's REFERENCE vocabulary, emitted by `Fuaran.Core.Idl.Codegen`'s F* target from the test project (Phase 173: the source is `tests/Fuaran.Core.Tests/ReferenceIdl.fs`, never the shared corpus). Carries `#set-options \\\"--ext context_pruning\\\"` INSIDE the generated file, like the hand-written modules that open the whole decode model and for the same reason. Four of five kinds are modelled — `Measure` is the one BOUNDARY, refused for its numeric default (`Fixed { value = 0.0 }`; the model's numeric carriers are opaque), which is the same class Phase 150 met at `Tabs.activeIndex` and is pinned as exactly that one kind by `IdlFStarTargetTests`. It is CHECKED but not EXTRACTED (`$proofOnly` in check.ps1): the decoder it models is one a generator emits into a consuming host, not one this repository ships. WHAT A BUMP MEANS HERE: its size is a function of the vocabulary it is generated from, so growth means the VOCABULARY grew — the emitted header names every kind modelled and every kind not — and the generation diff is what makes any such change deliberate. SEEDED from cold quaked runs on the pinned prover with the leg's own flags — one direct run and four `check.ps1 -Runs 3` legs on the same dev machine, idle for some and shared with a build for others (10s, 5s, 6s, 4s, 4s, 10s, 3s): slowest 10s, so the budget is 20s by the seeding rule; fastest 3s, so the floor is 0s by floorSeeding."
    },
    {
      "module": "VocabularyProofs",
      "budgetSeconds": 30,
      "measuredSeconds": 12,
      "floorSeconds": 2,
      "fastestSeconds": 5,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#168",
      "note": "The generated ROUND TRIP over `Vocabulary` (`dec_node (enc_node x) == Ok x` for every value of every modelled type), plus `dec_node_total`. Emitted by the same target from the same walk as the model, ONE LEMMA PER CONSTRUCTOR and one per PRESENCE PATTERN (Phase 168): `rt_<T>` is a case split whose arms cite `rt_<T>__<Ctor>`, one constructor's arm alone, and a constructor with `presenceSplitAt` (2) or more conditional members is split further into `…__p<bits>` lemmas whose `requires` pins every conditional member present or absent — so no query carries more than one constructor's object shapes, which is the structural fix Phase 150 named for the 2^k failure it measured at twenty UI kinds. The family is one mutual induction on the lexicographic measure `%[x; tier]`. The in-file `--z3rlimit 200` precedent is RETIRED with the shape that needed it: every query here discharges at the leg's own rlimit of 40, under `--quake 3`, cold. Twenty-six lemmas: the reference vocabulary reaches the split twice — the node envelope (hidden, label) and `Embed` (contentHash, props), four patterns each. MEASURED on the pinned prover, cold, with the leg's own flags (Phase 168, this dev machine, shared with a build for some runs): one direct run and three `check.ps1 -Runs 1` legs — 18s (direct), 13s, 11s, 12s. Slowest 18s, so the budget stays 40s by the seeding rule; Phase 173's 17s slowest for the one-lemma shape is the BEFORE. The floor is NOT re-seeded from these few runs: the new shape is strictly more work than the one Phase 173 observed at 5s fastest, so the 2s floor seeded then remains a true lower bound, and raising it from four samples on one machine would risk a red leg on a faster runner — the direction Phase 164's asymmetry says to fear. RE-SEEDED BY PHASE 182, downwards: the presence split is LINEAR in the conditional members now, so the 2^k pattern lemmas are gone and this script is nine `lk_…` lookups plus eighteen round-trip lemmas where it was twenty-six round-trip lemmas. Three cold quaked runs of one `check.ps1 -Runs 3` leg measured 12s, 12s, 12s — slowest 12s, so the budget is 30s by the seeding rule, down from 40s. The floor STAYS at 2s: every run observed is slower than the 5s `fastestSeconds` Phase 173 recorded, so `floorSeeding` forces nothing, and a floor raised from a loaded afternoon is the one direction that costs a false red."
    },
    {
      "module": "DocVocabulary",
      "budgetSeconds": 30,
      "measuredSeconds": 11,
      "floorSeconds": 0,
      "fastestSeconds": 3,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#173",
      "note": "The GENERATED model of the vendored SECOND-DOMAIN sample, emitted by `Fuaran.Core.Idl.Codegen`'s F* target from the test project (Phase 173: the source is `tests/Fuaran.Core.Tests/SecondDomainSpike.fs`, never the shared corpus). Carries `#set-options \\\"--ext context_pruning\\\"` INSIDE the generated file, like the hand-written modules that open the whole decode model and for the same reason. Every kind is modelled, and it is on the DECLARED non-default wire shape (bare-string discriminator, flat node envelope, declaration key order) — the shape the reference vocabulary does not reach, which is why the sample is in the set. It is CHECKED but not EXTRACTED (`$proofOnly` in check.ps1): the decoder it models is one a generator emits into a consuming host, not one this repository ships. WHAT A BUMP MEANS HERE: its size is a function of the vocabulary it is generated from, so growth means the VOCABULARY grew — the emitted header names every kind modelled and every kind not — and the generation diff is what makes any such change deliberate. SEEDED from cold quaked runs on the pinned prover with the leg's own flags — one direct run and four `check.ps1 -Runs 3` legs on the same dev machine, idle for some and shared with a build for others (5s, 5s, 5s, 4s, 11s, 6s, 3s): slowest 11s, so the budget is 30s by the seeding rule; fastest 3s, so the floor is 0s by floorSeeding."
    },
    {
      "module": "DocVocabularyProofs",
      "budgetSeconds": 20,
      "measuredSeconds": 10,
      "floorSeconds": 2,
      "fastestSeconds": 5,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#168",
      "note": "The generated ROUND TRIP over `DocVocabulary` (`dec_node (enc_node x) == Ok x` for every value of every modelled type), plus `dec_node_total`. Emitted by the same target from the same walk as the model, ONE LEMMA PER CONSTRUCTOR and one per PRESENCE PATTERN (Phase 168): `rt_<T>` is a case split whose arms cite `rt_<T>__<Ctor>`, one constructor's arm alone, and a constructor with `presenceSplitAt` (2) or more conditional members is split further into `…__p<bits>` lemmas whose `requires` pins every conditional member present or absent — so no query carries more than one constructor's object shapes, which is the structural fix Phase 150 named for the 2^k failure it measured at twenty UI kinds. The family is one mutual induction on the lexicographic measure `%[x; tier]`. The in-file `--z3rlimit 200` precedent is RETIRED with the shape that needed it: every query here discharges at the leg's own rlimit of 40, under `--quake 3`, cold. Twenty-seven lemmas; no constructor of the second-domain sample reaches the presence split (none carries two conditional members), so this module is the per-constructor shape alone — one arm lemma per kind, `rt_vkind` the case split. MEASURED (Phase 168) as for `VocabularyProofs`: 8s (direct), 10s, 10s, 8s. Slowest 10s, budget stays 20s (the 20s minimum); Phase 173's 9s slowest is the BEFORE. Floor kept at 2s for the reason `VocabularyProofs` gives. UNCHANGED BY PHASE 182, and it is the LOAD CONTROL for that phase's measurements. No constructor of the second-domain sample reaches the presence split, so the linear shape emits no lookup for it and this file changed in COMMENTS ONLY. It nonetheless measured 17s, 16s, 16s across Phase 182's `check.ps1 -Runs 3` leg against the 8-10s recorded here — the same content, about 1.7x slower — which is what says that leg was contended and is why the other two entries' re-seedings are conservative rather than optimistic. Budget deliberately NOT re-seeded from those readings, on the precedent `TreeOps` records: a run that inflates an untouched module measures the afternoon, not the module."
    },
    {
      "module": "ScoreVocabulary",
      "budgetSeconds": 40,
      "measuredSeconds": 16,
      "floorSeconds": 4,
      "fastestSeconds": 8,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#173",
      "note": "The GENERATED model of the vendored THIRD-DOMAIN sample, emitted by `Fuaran.Core.Idl.Codegen`'s F* target from the test project (Phase 173: the source is `tests/Fuaran.Core.Tests/ScoreDomainSpike.fs`, never the shared corpus). Carries `#set-options \\\"--ext context_pruning\\\"` INSIDE the generated file, like the hand-written modules that open the whole decode model and for the same reason. Nineteen of twenty-one kinds are modelled — `Note` and `Chord` meet the same numeric-default boundary (`voice = 1`) — and it is records and omit-at-default at scale on the non-default wire shape. The largest generated module here, and still an order of magnitude under the UI model it replaced. It is CHECKED but not EXTRACTED (`$proofOnly` in check.ps1): the decoder it models is one a generator emits into a consuming host, not one this repository ships. WHAT A BUMP MEANS HERE: its size is a function of the vocabulary it is generated from, so growth means the VOCABULARY grew — the emitted header names every kind modelled and every kind not — and the generation diff is what makes any such change deliberate. SEEDED from cold quaked runs on the pinned prover with the leg's own flags — one direct run and four `check.ps1 -Runs 3` legs on the same dev machine, idle for some and shared with a build for others (16s, 11s, 14s, 10s, 15s, 12s, 8s): slowest 16s, so the budget is 40s by the seeding rule; fastest 8s, so the floor is 4s by floorSeeding."
    },
    {
      "module": "ScoreVocabularyProofs",
      "budgetSeconds": 70,
      "measuredSeconds": 32,
      "floorSeconds": 6,
      "fastestSeconds": 12,
      "floorPhase": "fuaran-core#173",
      "phase": "fuaran-core#168",
      "note": "The generated ROUND TRIP over `ScoreVocabulary` (`dec_node (enc_node x) == Ok x` for every value of every modelled type), plus `dec_node_total`. Emitted by the same target from the same walk as the model, ONE LEMMA PER CONSTRUCTOR and one per PRESENCE PATTERN (Phase 168): `rt_<T>` is a case split whose arms cite `rt_<T>__<Ctor>`, one constructor's arm alone, and a constructor with `presenceSplitAt` (2) or more conditional members is split further into `…__p<bits>` lemmas whose `requires` pins every conditional member present or absent — so no query carries more than one constructor's object shapes, which is the structural fix Phase 150 named for the 2^k failure it measured at twenty UI kinds. The family is one mutual induction on the lexicographic measure `%[x; tier]`. The in-file `--z3rlimit 200` precedent is RETIRED with the shape that needed it: every query here discharges at the leg's own rlimit of 40, under `--quake 3`, cold. Sixty-two lemmas — the largest proof script in the leg: nineteen kind arms, and `Measure` and `Score` reach the presence split (omit-at-default members, so the pattern lemmas pin equality with the default the encoder tests, not `Some?`). MEASURED (Phase 168) as for `VocabularyProofs`: 58s (direct), 46s, 108s, 47s — the 108s leg ran the whole leg 37% slower than the other two, so it is load, and it is kept because the rule seeds from the slowest. Slowest 108s, so the budget is RAISED from 90s to 220s by the seeding rule — the price of forty-four more lemmas, each its own query, against Phase 173's 42s slowest for the one-lemma shape (the BEFORE). Floor kept at 6s for the reason `VocabularyProofs` gives. RE-SEEDED BY PHASE 182, downwards and by a lot: the split by PATTERN became a split by MEMBER, so the sixty-two lemmas are now thirteen `lk_…` lookups plus forty-two round-trip lemmas, and the forty-four extra queries Phase 168 paid for are not there to pay for. Three cold quaked runs of one `check.ps1 -Runs 3` leg measured 31s, 32s, 31s — slowest 32s, so the budget is 70s by the seeding rule, down from 220s. That leg was a LOADED one and the numbers still did not move: `Capability` (untouched) ran 75s against a 20s budget on the same run 3 and `JsonParse` (untouched) 76s against 42s on run 1, so the stability of this module's three readings is a fact about the module rather than about the afternoon. The floor STAYS at 6s: every run observed is slower than the 12s `fastestSeconds` recorded, so `floorSeeding` forces nothing."
    },
    {
      "module": "ColumnOps",
      "budgetSeconds": 100,
      "measuredSeconds": 47,
      "floorSeconds": 4,
      "fastestSeconds": 9,
      "floorPhase": "fuaran-core#176",
      "phase": "fuaran-core#176",
      "note": "The compute strand's op algebra — `ColumnOps.apply` / `canApply` / `invert` / `Diff.toOps` clause for clause, the five Preservation clauses proved for columns, and `Diff.toOps`'s reconstruction. Self-contained (opens nothing) and cheap: default context, no scoped options, the whole module discharges at the leg's own rlimit of 40 under `--quake 3`. SEEDED BY PHASE 176: three cold runs through the kit on this machine, 47s, 31s, 22s (the first carrying the prover's first-run cost, beside concurrent sessions), and the prover invoked directly on the same file with the same flags during authoring, fresh verification each time, 9-20s — so the budget is 2x the slowest kit run rounded up to the next 10s (100s) and the floor is half the fastest genuine run rounded down (4s from 9s), per the two rules above. The two proof shapes that cost anything are the well-formedness preservation for `AppendRows` (a forall over the columns, threaded through `typed_append_aux`) and the diff's two walks (`changed_apply`, `inserts_apply`), each an induction carrying an invariant about the prefix already processed."
    },
    {
      "module": "Capability",
      "budgetSeconds": 20,
      "measuredSeconds": 7,
      "floorSeconds": 3,
      "fastestSeconds": 6,
      "floorPhase": "fuaran-core#177",
      "phase": "fuaran-core#177",
      "note": "The function seam — the effect lattice, the value spaces, `Function.signature` / `apply` / `curry` / `compose` / `auditEffect` over an abstract witness, and `Capability.validateArgs` / `invoke` with `Registry.register` / `enumerate` / `dispatch`, clause for clause; default-deny dispatch, validation before invocation, enumeration equal to the registry and the three function laws proved. Self-contained (opens nothing) and the cheapest model in the leg: default context, no scoped options, the whole module discharges at the leg's own rlimit of 40 under `--quake 3` with no query needing more than one attempt. SEEDED BY PHASE 177: one cold run through the kit on this machine at 7s (a `-Runs 1` leg beside concurrent sessions), and the prover invoked directly on the same file with the same flags during authoring, a fresh verification each time, 6-8s — so the budget is the 20s minimum (2x7 is 14, under `seeding.minimumBudgetSeconds`) and the floor is half the fastest genuine run under the leg's flags rounded down (3s from 6s), per the two rules above. The two proof shapes that cost anything are the no-capture walk (`walk_single`, an induction over the declaration carrying the distinct-address invariant) and the join-fold characterisation (`fold_covers`, generalised over the accumulator). One authoring lesson is recorded in the README's theorem 10 section and is about closures rather than cost: a `List.tryFind (fun ...)` written once in a definition and again in its lemma is two terms to the encoding, and every such walk is a named function here."
    },
    {
      "module": "Propagation",
      "budgetSeconds": 20,
      "measuredSeconds": 4,
      "floorSeconds": 0,
      "fastestSeconds": 4,
      "floorPhase": "fuaran-core#186",
      "phase": "fuaran-core#186",
      "note": "The compute strand's incremental promise — `dependents`, `dirtyFromChangedIds` with its frontier loop, `staleSet`, and the `walk` / `eval` / `evalFrom` driver over an abstract node evaluator, clause for clause, with `sort`'s result a parameter; the dirty set proved sound and least, `evalFrom` proved equal to `eval` under the stated evaluator contract, reuse proved minimal and an unknown change proved refused. Self-contained (opens nothing) and among the cheapest models in the leg: default context, no scoped options, no SMT pattern, the whole module discharges at the leg's own rlimit of 40 under `--quake 3` with no query needing more than one attempt. SEEDED BY PHASE 186: three cold runs through the kit on this machine with no other prover running, 4s, 4s, 4s — so the budget is the 20s minimum (2x4 is 8, under `seeding.minimumBudgetSeconds`) and the floor is 0 by `floorSeeding.zeroBelowSeconds`: the fastest genuine cold run is under 5s, so half of it is inside process-start noise and a positive floor would be a coin toss. Like Skeleton's and Limits', that 0 is honest about covering nothing — what covers this module against a warm cache is the per-invocation cache directory, not this number. The three proof shapes that cost anything are the frontier loop's termination measure (`grow_measure`, which has to be established BEFORE the function it justifies and is then re-used by every induction over it), the closure invariant (`grow_closed`, closed except at the frontier), and the three-walk induction behind the agreement theorem (`go_agree`: the old full walk carried beside the two new ones, which hold the same results at every step). If this entry ever overshoots, look first for a lemma about `union` / `diff` / `app` that has gained an SMT pattern: they rewrite into one another and carry none, on the README's recorded lesson."
    },
    {
      "module": "Query",
      "budgetSeconds": 20,
      "measuredSeconds": 9,
      "floorSeconds": 4,
      "fastestSeconds": 8,
      "floorPhase": "fuaran-core#187",
      "phase": "fuaran-core#187",
      "note": "The data-acquisition seam — `Query.validateParams` / `invoke` / `invocationKey` with `QueryRegistry.register` / `enumerate` / `dispatch`, clause for clause over an abstract resolver and a `renderers` record; default-deny dispatch, validation before resolution, enumeration equal to the registry and the capture key's determinism proved, plus the two findings (`all_null_accepted`, `key_collision`). Self-contained (opens nothing), default context, no scoped options: the whole module discharges at the leg's own rlimit of 40 under `--quake 3` with no query needing more than one attempt. SEEDED BY PHASE 187 from three cold runs of the prover invoked directly on the file with the leg's own flags and a fresh cache each time — 8s, 9s, 9s — taken BESIDE up to three other proof workers on the same machine (a campaign tier), so by `seeding`'s rule the budget is the 20s minimum (2x9 is 18, under `seeding.minimumBudgetSeconds`) and by `floorSeeding`'s the floor is half the fastest genuine cold run rounded down (4s from 8s). A quiet machine will run it faster than 8s; per `floorSeeding` that leaves the floor correct and merely conservative unless a run lands under 4s, which is first a reason to look for a second writer. The one proof shape that costs anything is the sort's uniqueness (`sorted_unique`: two name-sorted lists with distinct names and the same bindings are the same list), an induction over both lists carrying a quantified membership hypothesis that is instantiated BY HAND at each head rather than left to the solver's pattern matching. The one authoring slip worth recording was a termination measure: `sorted_head_le` recurses on the tail while its head argument CHANGES, so the default lexicographic measure over the arguments in order refuses it and `(decreases t)` says what is actually shrinking."
    }
  ]
}
